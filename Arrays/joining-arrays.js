const numbers = [1, 2, 3];
console.log(numbers);
const joined = numbers.join(',');
console.log(joined);


const message = 'This is my first message';
const parts = message.split(' ');
const hyphenParts = parts.join('-');
const partsAnother = message.split('');

console.log(parts);
console.log(partsAnother);
console.log(hyphenParts);