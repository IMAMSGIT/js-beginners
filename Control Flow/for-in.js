let person = {
    name: 'Adiat',
    age: 33
};

for (let firstPart in person)
    console.log(firstPart, person[firstPart]);


const colors = ['red', 'green', 'blue']
for (let index in colors)
    console.log(index)